
<%= simple_form_for(@flat) do |f| %>

  <%= f.input :television, as: :boolean %>
  <%= f.input :pet, as: :boolean %>
  <%= f.input :bed, as: :boolean %>
  <%= f.input :wifi, as: :boolean %>
  <%= f.input :user, as: :boolean %>
  <%= f.input :document, as: :boolean %>
<% end %>

